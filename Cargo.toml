[workspace]

members = [
    "credential",
    "utils",
    "etag",
    "upload_token",
    "http",
    "http-reqwest",
    "http-isahc",
    # "http-client",
]

[profile.release]
lto = "fat"
codegen-units = 1
